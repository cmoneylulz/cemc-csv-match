#ifndef VALUETABLE_H_
#define VALUETABLE_H_

#include <iostream>
#include <stdio.h>
#include <string>
#include <vector>
#include <fstream>

using namespace std;

#include "HelperMethods.h"

using namespace utility;

namespace model 
{
    class ValueTable
    {
    private:

    public:
        ValueTable();
        virtual ~ValueTable();
    };
}

#endif
